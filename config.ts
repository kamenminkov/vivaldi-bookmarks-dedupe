export const WRITE_CLEAN_BOOKMARKS_IN_PLACE: boolean = false; // whether to write cleaned up bookmarks back in their original location - useful when multiple paths are provided

export const DEFAULT_BOOKMARKS_INPUT_FILENAME: string = 'Bookmarks';
export const DEFAULT_NOTES_INPUT_FILENAME: string = 'Notes';

export const PROCESS_NOTES: boolean = false;
export const PROCESS_BOOKMARKS: boolean = true;
